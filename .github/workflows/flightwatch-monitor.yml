name: FlightWatch hourly monitor

on:
  schedule:
    - cron: '17 * * * *'
  workflow_dispatch:

jobs:
  monitor:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger FlightWatch monitoring
        run: |
          curl --fail --show-error --silent \
            --request POST \
            --header "x-cron-secret: ${{ secrets.CRON_SECRET }}" \
            "${{ secrets.FLIGHTWATCH_URL }}/api/cron/check"
